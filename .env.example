# Copy to .env and fill in. NEVER commit .env (already gitignored).
GEMINI_API_KEY=your-key-here
# Gemini 3.5 Flash (GA). Override only if your key targets a different model.
GEMINI_MODEL=gemini-3.5-flash
