# Deriv AI Engineer Assessment

> Fill in the TODOs once the task is known.

## What this does
TODO

## Design
TODO  (message -> [guardrail] -> model structured output -> validate -> result | safe fallback)

## Setup & run
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # then add your GEMINI_API_KEY
python check_key.py           # verify connectivity
```
TODO: app run command

## Assumptions
TODO

## Tradeoffs / cut for time
TODO

## What I'd do next
TODO  (evals in CI, Docker for deploy, observability, guardrails)
